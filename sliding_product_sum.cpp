#include <bits/stdc++.h>

 
 using namespace std;
 typedef long long int lli;
 lli n,m
 int k;
 lli mul(lli a , lli b){
 	for(lli i =  0 ; i < 63 ; ++i){
		if(b)
	}
 }
 int main(){
 	scanf("%lld%d%lld",&n,&k,&m);
	if(K == N){
		for()
	}	
 }
